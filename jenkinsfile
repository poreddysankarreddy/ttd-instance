pipeline {
	agent {  label 'linux-node' }
	stages {
		stage('---clean---'){
			tools {
				maven 'mvn3.8.5'
			}
			steps {
				
				sh "mvn clean"
			}
		}
		stage('Test') {
			tools {
				maven 'mvn3.8.3'
			}
			steps {
				
				sh "make test"
			}
		}
		stage('---package---'){
			
			steps {
				
				sh "mvn package"
			}
		}
	}
	post {
		success {
			echo 'job was built successfully'
		}
		failure {
			echo 'job was not build..it was failed'
		}
	}
}
